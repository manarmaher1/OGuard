[profile.default]
src = "src"
out = "out"
libs = ["lib"]
test = "test"
cache_path = "cache"
solc_version = "0.8.20"
optimizer = true
optimizer_runs = 200

[profile.default.model_checker]
contracts = { 'src/OGuard.sol' = [ 'OGuard' ] }
engine = 'chc'
targets = [ 'assert' ]
timeout = 10000
